name: Carga Env Vars

on: 
  workflow_dispatch:

jobs:
  env-vars:
    name: Vars
    runs-on: 'ubuntu-latest'
    steps:
      - name: Set env
        run: test_var="test"
        shell: bash
      - name: Test bash
        run: echo "$test_var"
        shell: bash
        
